{
  "name": "immich-smart-album",
  "version": "1.0.0",
  "description": "An Immich workflow filter that matches assets by image content using the server's own smart search.",
  "license": "AGPL-3.0",
  "private": true,
  "scripts": {
    "prepare-build": "node scripts/prepare-build.mjs",
    "build:js": "npm run prepare-build && tsc --noEmit && node esbuild.js",
    "build:wasm": "extism-js dist/index.js -i dist/index.d.ts -o dist/plugin.wasm",
    "build": "npm run build:js && npm run build:wasm",
    "test": "node test/run.mjs"
  },
  "devDependencies": {
    "@extism/extism": "^2.0.0-rc13",
    "@extism/js-pdk": "^1.1.1",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
